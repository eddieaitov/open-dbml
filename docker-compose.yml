version: "3.9"

services:
  open-dbml:
    build: .
    container_name: open-dbml
    ports:
      - "7924:7924"
    volumes:
      # Монтируем src/ чтобы редактировать файлы снаружи
      - ./src:/app/src
      - ./serve.js:/app/serve.js
    environment:
      - NODE_ENV=development
    restart: unless-stopped
